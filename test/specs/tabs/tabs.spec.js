import React from 'react';
import { Tabs } from 'components';
import { assert } from 'chai';
import { shallow } from 'enzyme';

describe('<Tabs/>', () => {
  it('exists', () => {
    assert(Tabs);
  })
});

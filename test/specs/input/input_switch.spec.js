import React from 'react'
import $ from 'jquery'
import Realize from 'realize'
import {InputSwitch} from 'components/input'
import {assert} from 'chai';
import {shallow} from 'enzyme';


describe('<InputSwitch/>', () => {
  it('exists', () => {
    expect(InputSwitch).to.exists;
  });
});
